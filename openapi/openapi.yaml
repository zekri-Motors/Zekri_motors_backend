openapi: 3.0.3
info:
  title: Zekri Motors Backend API
  version: 1.1.0
  description: |
    ملف OpenAPI محدث حسب routes/api.php الحالي لاستخدامه في Bruno أو أي API client.

    طريقة العمل في Bruno:
    1. استورد هذا الملف.
    2. نفذ طلب `POST /auth/login`.
    3. احفظ قيمة `token` من الاستجابة في Runtime Variable باسم `token`.
    4. كل الطلبات المحمية تستخدم `Authorization: Bearer {{token}}`.

    سكربت Bruno المناسب في تبويب Post Response لطلب login:
    ```js
    bru.setVar("token", res.body.token);
    ```
servers:
  - url: http://localhost:8000/api
    description: Local Laravel server
  - url: https://your-domain.com/api
    description: Production

security:
  - bearerAuth: []

tags:
  - name: Auth
  - name: Public
  - name: Suppliers
  - name: SupplierPayments
  - name: Batches
  - name: Cars
  - name: CarMedia
  - name: GeneralMedia
  - name: Tags
  - name: Contacts
  - name: ContainerOpeners
  - name: ServiceProviders
  - name: Customers
  - name: Agents
  - name: Orders
  - name: CustomerPayments
  - name: AgentTransactions
  - name: Expenses
  - name: Invoices
  - name: Dashboard
  - name: Treasury
  - name: Users
  - name: Settings
  - name: PreOrderCars
  - name: Documents

x-bruno:
  auth:
    type: bearer
    token: "{{token}}"
  login:
    request: POST /auth/login
    postResponseScript: bru.setVar("token", res.body.token);

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: Sanctum
      description: استخدم Bearer token الناتج من POST /auth/login. في Bruno ضع القيمة `{{token}}`.
      x-bruno:
        token: "{{token}}"
  parameters:
    Page: { name: page, in: query, schema: { type: integer, default: 1 } }
    PerPage: { name: per_page, in: query, schema: { type: integer, default: 15 } }
    Search: { name: search, in: query, schema: { type: string } }
  responses:
    Ok: { description: OK }
    Created: { description: Created }
    Deleted: { description: Deleted }
    Unauthenticated: { description: Unauthenticated }
    Forbidden: { description: Forbidden }
    NotFound: { description: Not found }
    ValidationError:
      description: Validation error
      content:
        application/json:
          schema:
            type: object
            properties:
              message: { type: string }
              errors:
                type: object
                additionalProperties:
                  type: array
                  items: { type: string }
  schemas:
    AnyJson: { type: object, additionalProperties: true }
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email: { type: string, format: email, example: superadmin@example.com }
        password: { type: string, example: ChangeMe123! }
        device_name: { type: string, example: bruno }
    RegisterRequest:
      type: object
      required: [name, email]
      properties:
        name: { type: string }
        email: { type: string, format: email }
        phone: { type: string, nullable: true }
        national_id: { type: string, nullable: true }
        passport_no: { type: string, nullable: true }
        address: { type: string, nullable: true }
    LoginResponse:
      type: object
      properties:
        message: { type: string }
        user: { $ref: '#/components/schemas/AnyJson' }
        token: { type: string }
    PartyRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        phone: { type: string, nullable: true }
        email: { type: string, format: email, nullable: true }
        address: { type: string, nullable: true }
        notes: { type: string, nullable: true }
    ServiceProviderRequest:
      allOf:
        - $ref: '#/components/schemas/PartyRequest'
        - type: object
          required: [provider_type]
          properties:
            provider_type: { type: string }
    ContactRequest:
      type: object
      required: [name, whatsapp_number]
      properties:
        name: { type: string }
        whatsapp_number: { type: string, example: "+213 555 12 34 56" }
        address: { type: string, nullable: true }
    BatchRequest:
      type: object
      required: [supplier_id]
      properties:
        supplier_id: { type: integer }
        purchase_date: { type: string, format: date, nullable: true }
        status: { type: string, enum: [partial, fully_paid], nullable: true }
        notes: { type: string, nullable: true }
    CarRequest:
      type: object
      required: [batch_id, supplier_id, brand, model, manufacture_year, foreign_purchase_price, sale_price]
      properties:
        batch_id: { type: integer }
        supplier_id: { type: integer }
        container_opener_id: { type: integer, nullable: true }
        brand: { type: string }
        model: { type: string }
        finition: { type: string, nullable: true }
        manufacture_year: { type: integer }
        color: { type: string, nullable: true }
        vin: { type: string, nullable: true }
        foreign_purchase_price: { type: number }
        shipping_cost: { type: number, nullable: true }
        sale_price: { type: number }
        tracking_number: { type: string, nullable: true }
        container_no: { type: string, nullable: true }
        shipping_date: { type: string, format: date, nullable: true }
        arrival_date: { type: string, format: date, nullable: true }
        delivery_date: { type: string, format: date, nullable: true }
        status: { type: string, enum: [available, shipping, in_show_room, delivered, sold], nullable: true }
        notes: { type: string, nullable: true }
    CustomerRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        phone: { type: string, nullable: true }
        email: { type: string, format: email, nullable: true }
        national_id: { type: string, nullable: true }
        passport_no: { type: string, nullable: true }
        address: { type: string, nullable: true }
        agent_id: { type: integer, nullable: true }
    AgentRequest:
      type: object
      required: [name, email, password]
      properties:
        name: { type: string }
        phone: { type: string, nullable: true }
        email: { type: string, format: email }
        password: { type: string, minLength: 8 }
        is_active: { type: boolean }
        address: { type: string, nullable: true }
        notes: { type: string, nullable: true }
    OrderRequest:
      type: object
      required: [customer_id, car_id]
      properties:
        customer_id: { type: integer }
        car_id: { type: integer }
        agent_id: { type: integer, nullable: true }
        paid_amount: { type: number, nullable: true }
        purchase_date: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true }
    CustomerPaymentRequest:
      type: object
      required: [order_id, customer_id, amount, payment_date]
      properties:
        order_id: { type: integer }
        customer_id: { type: integer }
        amount: { type: number }
        agent_id: { type: integer, nullable: true }
        attachment: { type: string, nullable: true }
        payment_date: { type: string, format: date }
        notes: { type: string, nullable: true }
    AgentTransactionRequest:
      type: object
      required: [agent_id, direction, amount, transaction_date, notes]
      properties:
        agent_id: { type: integer }
        direction: { type: string, enum: [in, out] }
        amount: { type: number }
        transaction_date: { type: string, format: date }
        attachment: { type: string, nullable: true }
        notes: { type: string }
    PreOrderCarRequest:
      type: object
      required: [brand, model, manufacture_year, price, customs_fees]
      properties:
        brand: { type: string }
        model: { type: string }
        finition: { type: string, nullable: true }
        manufacture_year: { type: integer }
        color: { type: string, nullable: true }
        price: { type: number }
        customs_fees: { type: number }
        notes: { type: string, nullable: true }
    MediaUpdateRequest:
      type: object
      properties:
        title: { type: string, nullable: true }
        description: { type: string, nullable: true }
        is_cover: { type: boolean, nullable: true }
        sort_order: { type: integer, nullable: true }
        tags:
          type: array
          items: { type: string }

  requestBodies:
    JsonParty: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PartyRequest' } } } }
    JsonServiceProvider: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ServiceProviderRequest' } } } }
    JsonContact: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ContactRequest' } } } }
    JsonBatch: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/BatchRequest' } } } }
    JsonCar: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CarRequest' } } } }
    JsonCustomer: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CustomerRequest' } } } }
    JsonAgent: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/AgentRequest' } } } }
    JsonOrder: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/OrderRequest' } } } }
    JsonCustomerPayment: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CustomerPaymentRequest' } } } }
    JsonAgentTransaction: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/AgentTransactionRequest' } } } }
    JsonPreOrderCar: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PreOrderCarRequest' } } } }
    JsonMediaUpdate: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/MediaUpdateRequest' } } } }
    BatchImport:
      required: true
      content:
        multipart/form-data:
          schema:
            type: object
            required: [supplier_id, file]
            properties:
              supplier_id: { type: integer }
              container_opener_id: { type: integer, nullable: true }
              purchase_date: { type: string, format: date, nullable: true }
              total_cost_foreign: { type: number, nullable: true }
              notes: { type: string, nullable: true }
              file: { type: string, format: binary }
    PreOrderImport:
      required: true
      content:
        multipart/form-data:
          schema:
            type: object
            required: [file]
            properties:
              file: { type: string, format: binary }
    MediaUpload:
      required: true
      content:
        multipart/form-data:
          schema:
            type: object
            description: أرسل واحدًا فقط من file أو url.
            properties:
              file: { type: string, format: binary, nullable: true }
              url: { type: string, format: uri, nullable: true }
              type: { type: string, enum: [image, video], nullable: true }
              title: { type: string, nullable: true }
              description: { type: string, nullable: true }
              is_cover: { type: boolean, nullable: true }
              sort_order: { type: integer, nullable: true }
              tags: { type: array, items: { type: string } }
    Expense:
      required: true
      content:
        multipart/form-data:
          schema:
            type: object
            required: [expense_type, amount, expense_date]
            properties:
              car_id: { type: integer, nullable: true }
              order_id: { type: integer, nullable: true }
              service_provider_id: { type: integer, nullable: true }
              expense_type: { type: string }
              amount: { type: number }
              attachment: { type: string, format: binary, nullable: true }
              expense_date: { type: string, format: date }
              notes: { type: string, nullable: true }

paths:
  /auth/register:
    post:
      tags: [Auth]
      security: []
      summary: Register public customer profile
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/RegisterRequest' } } } }
      responses: { '201': { $ref: '#/components/responses/Created' }, '422': { $ref: '#/components/responses/ValidationError' } }
  /auth/login:
    post:
      tags: [Auth]
      security: []
      summary: Login and get Sanctum token
      description: ضع سكربت Bruno Post Response `bru.setVar("token", res.body.token);`.
      x-bruno:
        postResponseScript: bru.setVar("token", res.body.token);
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/LoginRequest' } } } }
      responses:
        '200': { description: Login success, content: { application/json: { schema: { $ref: '#/components/schemas/LoginResponse' } } } }
        '422': { $ref: '#/components/responses/ValidationError' }
  /auth/me:
    get: { tags: [Auth], summary: Current user, responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /auth/logout:
    post: { tags: [Auth], summary: Logout current token, responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /auth/logout-all:
    post: { tags: [Auth], summary: Logout all tokens, responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /auth/update-password:
    post: &updatePassword
      tags: [Auth]
      summary: Update password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [current_password, password, password_confirmation]
              properties:
                current_password: { type: string }
                password: { type: string, minLength: 8 }
                password_confirmation: { type: string }
      responses: { '200': { $ref: '#/components/responses/Ok' } }
    put: *updatePassword
  /auth/update-profile:
    post: &updateProfile
      tags: [Auth]
      summary: Update profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                email: { type: string, format: email }
                phone: { type: string, nullable: true }
      responses: { '200': { $ref: '#/components/responses/Ok' } }
    put: *updateProfile
  /ping:
    get: { tags: [Auth], summary: Auth ping, responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /lookup/customer/{passport_no}:
    get:
      tags: [Public]
      security: []
      summary: Public customer lookup by passport path param
      parameters: [{ name: passport_no, in: path, required: true, schema: { type: string } }]
      responses: { '200': { $ref: '#/components/responses/Ok' } }
  /lookup/customer:
    get:
      tags: [Public]
      security: []
      summary: Public customer lookup by query
      parameters: [{ name: passport_no, in: query, schema: { type: string } }]
      responses: { '200': { $ref: '#/components/responses/Ok' } }
  /cars/available:
    get:
      tags: [Public, Cars]
      security: []
      summary: Public available cars catalogue
      parameters: [{ $ref: '#/components/parameters/Search' }, { $ref: '#/components/parameters/PerPage' }]
      responses: { '200': { $ref: '#/components/responses/Ok' } }
  /suppliers:
    get: { tags: [Suppliers], summary: List suppliers, parameters: [{ $ref: '#/components/parameters/Search' }, { $ref: '#/components/parameters/PerPage' }, { $ref: '#/components/parameters/Page' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [Suppliers], summary: Create supplier, requestBody: { $ref: '#/components/requestBodies/JsonParty' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /suppliers/{supplier}:
    parameters: [{ name: supplier, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Suppliers], summary: Show supplier, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: { tags: [Suppliers], summary: Update supplier, requestBody: { $ref: '#/components/requestBodies/JsonParty' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    patch: { tags: [Suppliers], summary: Update supplier, requestBody: { $ref: '#/components/requestBodies/JsonParty' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [Suppliers], summary: Delete supplier, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /supplier-payments:
    get: { tags: [SupplierPayments], summary: List supplier payments, parameters: [{ name: supplier_id, in: query, schema: { type: integer } }, { name: batch_id, in: query, schema: { type: integer } }, { $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post:
      tags: [SupplierPayments]
      summary: Create supplier payment and distribute it
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [supplier_id, amount_foreign, exchange_rate, payment_date]
              properties:
                supplier_id: { type: integer }
                batch_id: { type: integer, nullable: true }
                amount_foreign: { type: number }
                exchange_rate: { type: number }
                payment_date: { type: string, format: date }
                notes: { type: string, nullable: true }
      responses: { '201': { $ref: '#/components/responses/Created' } }
  /supplier-payments/{supplier_payment}:
    parameters: [{ name: supplier_payment, in: path, required: true, schema: { type: integer } }]
    get: { tags: [SupplierPayments], summary: Show supplier payment, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: { tags: [SupplierPayments], summary: Update supplier payment, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    patch: { tags: [SupplierPayments], summary: Update supplier payment, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [SupplierPayments], summary: Delete supplier payment, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /batches:
    get: { tags: [Batches], summary: List batches, parameters: [{ $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [Batches], summary: Create batch, requestBody: { $ref: '#/components/requestBodies/JsonBatch' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /batches/import:
    post: { tags: [Batches], summary: Import batch cars from xlsx/xls/csv, requestBody: { $ref: '#/components/requestBodies/BatchImport' }, responses: { '201': { $ref: '#/components/responses/Created' }, '422': { $ref: '#/components/responses/ValidationError' } } }
  /batches/{batch}:
    parameters: [{ name: batch, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Batches], summary: Show batch, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: { tags: [Batches], summary: Update batch, requestBody: { $ref: '#/components/requestBodies/JsonBatch' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    patch: { tags: [Batches], summary: Update batch, requestBody: { $ref: '#/components/requestBodies/JsonBatch' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [Batches], summary: Delete batch, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /cars:
    get: { tags: [Cars], summary: List cars, parameters: [{ $ref: '#/components/parameters/Search' }, { $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [Cars], summary: Create car, requestBody: { $ref: '#/components/requestBodies/JsonCar' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /cars/table:
    get: { tags: [Cars], summary: Flat cars table report, responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /cars/table/export:
    get: { tags: [Cars], summary: Export flat cars table, responses: { '200': { description: File download } } }
  /cars/{car}:
    parameters: [{ name: car, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Cars], summary: Show car, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: { tags: [Cars], summary: Update car, requestBody: { $ref: '#/components/requestBodies/JsonCar' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    patch: { tags: [Cars], summary: Update car, requestBody: { $ref: '#/components/requestBodies/JsonCar' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [Cars], summary: Delete car, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /cars/{car}/expenses:
    parameters: [{ name: car, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Cars], summary: List car expenses, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post:
      tags: [Cars]
      summary: Add car expense
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [expense_type, local_amount]
              properties:
                expense_type: { type: string }
                foreign_amount: { type: number, nullable: true }
                local_amount: { type: number }
                notes: { type: string, nullable: true }
      responses: { '201': { $ref: '#/components/responses/Created' } }
  /cars/{car}/ownership-history:
    parameters: [{ name: car, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Cars], summary: Car ownership history, responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /cars/{car}/documents:
    parameters: [{ name: car, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Documents], summary: List car documents, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post:
      tags: [Documents]
      summary: Add car document
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, file_path]
              properties:
                title: { type: string }
                file_path: { type: string }
      responses: { '201': { $ref: '#/components/responses/Created' } }
  /cars/{car}/documents/{document}:
    parameters: [{ name: car, in: path, required: true, schema: { type: integer } }, { name: document, in: path, required: true, schema: { type: integer } }]
    delete: { tags: [Documents], summary: Delete car document, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /cars/{car}/media:
    parameters: [{ name: car, in: path, required: true, schema: { type: integer } }]
    get: { tags: [CarMedia], summary: List media for a car, parameters: [{ name: type, in: query, schema: { type: string, enum: [image, video] } }, { name: tags, in: query, schema: { type: string }, description: Comma separated tag names }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [CarMedia], summary: Upload/link media to car, requestBody: { $ref: '#/components/requestBodies/MediaUpload' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /car-media/search:
    get: { tags: [CarMedia], summary: Search car media globally, parameters: [{ name: car_name, in: query, schema: { type: string } }, { name: type, in: query, schema: { type: string, enum: [image, video] } }, { name: tags, in: query, schema: { type: string } }, { $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /car-media/{carMedia}:
    parameters: [{ name: carMedia, in: path, required: true, schema: { type: integer } }]
    put: { tags: [CarMedia], summary: Update car media metadata/tags, requestBody: { $ref: '#/components/requestBodies/JsonMediaUpdate' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [CarMedia], summary: Delete car media, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /general-media:
    get: { tags: [GeneralMedia], summary: List general media, parameters: [{ name: q, in: query, schema: { type: string } }, { name: type, in: query, schema: { type: string, enum: [image, video] } }, { name: tags, in: query, schema: { type: string } }, { $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [GeneralMedia], summary: Upload/link general media, requestBody: { $ref: '#/components/requestBodies/MediaUpload' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /general-media/{generalMedia}:
    parameters: [{ name: generalMedia, in: path, required: true, schema: { type: integer } }]
    put: { tags: [GeneralMedia], summary: Update general media metadata/tags, requestBody: { $ref: '#/components/requestBodies/JsonMediaUpdate' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [GeneralMedia], summary: Delete general media, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /tags:
    get: { tags: [Tags], summary: Autocomplete/list tags, parameters: [{ name: q, in: query, schema: { type: string } }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /container-openers:
    get: { tags: [ContainerOpeners], summary: List container openers, parameters: [{ $ref: '#/components/parameters/Search' }, { $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [ContainerOpeners], summary: Create container opener, requestBody: { $ref: '#/components/requestBodies/JsonParty' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /container-openers/{container_opener}:
    parameters: [{ name: container_opener, in: path, required: true, schema: { type: integer } }]
    get: { tags: [ContainerOpeners], summary: Show container opener, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: { tags: [ContainerOpeners], summary: Update container opener, requestBody: { $ref: '#/components/requestBodies/JsonParty' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    patch: { tags: [ContainerOpeners], summary: Update container opener, requestBody: { $ref: '#/components/requestBodies/JsonParty' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [ContainerOpeners], summary: Delete container opener, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /service-providers:
    get: { tags: [ServiceProviders], summary: List service providers, parameters: [{ $ref: '#/components/parameters/Search' }, { $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [ServiceProviders], summary: Create service provider, requestBody: { $ref: '#/components/requestBodies/JsonServiceProvider' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /service-providers/{service_provider}:
    parameters: [{ name: service_provider, in: path, required: true, schema: { type: integer } }]
    get: { tags: [ServiceProviders], summary: Show service provider, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: { tags: [ServiceProviders], summary: Update service provider, requestBody: { $ref: '#/components/requestBodies/JsonServiceProvider' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    patch: { tags: [ServiceProviders], summary: Update service provider, requestBody: { $ref: '#/components/requestBodies/JsonServiceProvider' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [ServiceProviders], summary: Delete service provider, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /customers:
    get: { tags: [Customers], summary: List customers, parameters: [{ $ref: '#/components/parameters/Search' }, { $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [Customers], summary: Create customer, requestBody: { $ref: '#/components/requestBodies/JsonCustomer' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /customers/{customer}:
    parameters: [{ name: customer, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Customers], summary: Show customer, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: { tags: [Customers], summary: Update customer, requestBody: { $ref: '#/components/requestBodies/JsonCustomer' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    patch: { tags: [Customers], summary: Update customer, requestBody: { $ref: '#/components/requestBodies/JsonCustomer' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [Customers], summary: Delete customer, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /customers/{customer}/documents:
    parameters: [{ name: customer, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Customers], summary: List customer documents, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post:
      tags: [Customers]
      summary: Upload customer documents
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                documents:
                  type: array
                  items: { type: string, format: binary }
      responses: { '201': { $ref: '#/components/responses/Created' } }
  /customers/{customer}/documents/{document}:
    parameters: [{ name: customer, in: path, required: true, schema: { type: integer } }, { name: document, in: path, required: true, schema: { type: integer } }]
    delete: { tags: [Customers], summary: Delete customer document, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /agents:
    get: { tags: [Agents], summary: List agents, parameters: [{ $ref: '#/components/parameters/Search' }, { $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [Agents], summary: Create agent with user account, requestBody: { $ref: '#/components/requestBodies/JsonAgent' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /agents/{agent}:
    parameters: [{ name: agent, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Agents], summary: Show agent, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: { tags: [Agents], summary: Update agent, requestBody: { $ref: '#/components/requestBodies/JsonAgent' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    patch: { tags: [Agents], summary: Update agent, requestBody: { $ref: '#/components/requestBodies/JsonAgent' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [Agents], summary: Delete agent, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /agents/{agent}/customers:
    parameters: [{ name: agent, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Agents], summary: Agent customers, responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /agents/{agent}/statement:
    parameters: [{ name: agent, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Agents], summary: Agent statement, responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /orders:
    get: { tags: [Orders], summary: List orders, parameters: [{ $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [Orders], summary: Create order, requestBody: { $ref: '#/components/requestBodies/JsonOrder' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /orders/{order}:
    parameters: [{ name: order, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Orders], summary: Show order, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: { tags: [Orders], summary: Update order notes/agent, requestBody: { $ref: '#/components/requestBodies/JsonOrder' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    patch: { tags: [Orders], summary: Update order notes/agent, requestBody: { $ref: '#/components/requestBodies/JsonOrder' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [Orders], summary: Delete order, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /orders/{order}/status:
    parameters: [{ name: order, in: path, required: true, schema: { type: integer } }]
    patch:
      tags: [Orders]
      summary: Change order status forward only
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { type: string, enum: [purchased, shipping, arrived_at_port, ready_for_delivery, delivered] }
                date: { type: string, format: date, nullable: true }
      responses: { '200': { $ref: '#/components/responses/Ok' }, '422': { $ref: '#/components/responses/ValidationError' } }
  /customer-payments:
    get: { tags: [CustomerPayments], summary: List customer payments, parameters: [{ name: order_id, in: query, schema: { type: integer } }, { name: is_remitted, in: query, schema: { type: boolean } }, { $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [CustomerPayments], summary: Create customer payment, requestBody: { $ref: '#/components/requestBodies/JsonCustomerPayment' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /customer-payments/remit:
    post:
      tags: [CustomerPayments]
      summary: Agent remits balance to company treasury
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount]
              properties:
                amount: { type: number }
                transaction_date: { type: string, format: date, nullable: true }
                attachment: { type: string, nullable: true }
                notes: { type: string, nullable: true }
      responses: { '200': { $ref: '#/components/responses/Ok' } }
  /customer-payments/{customer_payment}:
    parameters: [{ name: customer_payment, in: path, required: true, schema: { type: integer } }]
    get: { tags: [CustomerPayments], summary: Show customer payment, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [CustomerPayments], summary: Delete customer payment, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /customer-payments/{customer_payment}/transfer-to-treasury:
    parameters: [{ name: customer_payment, in: path, required: true, schema: { type: integer } }]
    post:
      tags: [CustomerPayments]
      summary: Approve/transfer customer payment to treasury
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                approval_date: { type: string, format: date, nullable: true }
                notes: { type: string, nullable: true }
      responses: { '200': { $ref: '#/components/responses/Ok' } }
  /agent-transactions:
    get: { tags: [AgentTransactions], summary: List agent transactions or pending remittances, parameters: [{ name: agent_id, in: query, schema: { type: integer } }, { name: is_remitted, in: query, schema: { type: boolean } }, { $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [AgentTransactions], summary: Create manual agent transaction, requestBody: { $ref: '#/components/requestBodies/JsonAgentTransaction' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /agent-transactions/{agent_transaction}:
    parameters: [{ name: agent_transaction, in: path, required: true, schema: { type: integer } }]
    get: { tags: [AgentTransactions], summary: Show agent transaction, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [AgentTransactions], summary: Delete manual agent transaction, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /agent-transactions/{agent_transaction}/approve-remittance:
    parameters: [{ name: agent_transaction, in: path, required: true, schema: { type: integer } }]
    post:
      tags: [AgentTransactions]
      summary: Approve agent remittance
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                approval_date: { type: string, format: date, nullable: true }
                notes: { type: string, nullable: true }
      responses: { '200': { $ref: '#/components/responses/Ok' } }
  /expenses:
    get: { tags: [Expenses], summary: List expenses, parameters: [{ name: car_id, in: query, schema: { type: integer } }, { name: order_id, in: query, schema: { type: integer } }, { $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [Expenses], summary: Create expense, requestBody: { $ref: '#/components/requestBodies/Expense' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /expenses/{expense}:
    parameters: [{ name: expense, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Expenses], summary: Show expense, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: { tags: [Expenses], summary: Update expense, requestBody: { $ref: '#/components/requestBodies/Expense' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    patch: { tags: [Expenses], summary: Update expense, requestBody: { $ref: '#/components/requestBodies/Expense' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [Expenses], summary: Delete expense, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /invoices:
    get: { tags: [Invoices], summary: List invoices, parameters: [{ $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post:
      tags: [Invoices]
      summary: Create invoice from order
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [order_id]
              properties: { order_id: { type: integer } }
      responses: { '201': { $ref: '#/components/responses/Created' } }
  /invoices/{invoice}:
    parameters: [{ name: invoice, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Invoices], summary: Show invoice, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [Invoices], summary: Delete invoice, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /invoices/{invoice}/refresh:
    parameters: [{ name: invoice, in: path, required: true, schema: { type: integer } }]
    post: { tags: [Invoices], summary: Refresh invoice from order totals, responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /dashboard:
    get: { tags: [Dashboard], summary: Dashboard stats, responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /tresury/summary:
    get:
      tags: [Treasury]
      summary: Treasury summary and movements
      description: Route name is intentionally `tresury` as defined in routes/api.php.
      parameters:
        - { name: date_from, in: query, schema: { type: string, format: date } }
        - { name: date_to, in: query, schema: { type: string, format: date } }
        - { name: direction, in: query, schema: { type: string, enum: [in, out] } }
        - { $ref: '#/components/parameters/PerPage' }
      responses: { '200': { $ref: '#/components/responses/Ok' } }
  /treasury/deposit:
    post:
      tags: [Treasury]
      summary: Manual treasury deposit
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount, transaction_date]
              properties:
                amount: { type: number }
                transaction_date: { type: string, format: date }
                notes: { type: string, nullable: true }
                attachment: { type: string, nullable: true }
      responses: { '201': { $ref: '#/components/responses/Created' } }
  /users:
    get: { tags: [Users], summary: List users, parameters: [{ name: role, in: query, schema: { type: string, enum: [super-admin, admin, agent] } }, { $ref: '#/components/parameters/Search' }, { $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post:
      tags: [Users]
      summary: Create staff user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, email, password, role]
              properties:
                name: { type: string }
                email: { type: string, format: email }
                phone: { type: string, nullable: true }
                password: { type: string, minLength: 8 }
                role: { type: string, enum: [super-admin, admin, agent] }
                is_active: { type: boolean }
                agent:
                  type: object
                  nullable: true
                  properties:
                    name: { type: string }
                    phone: { type: string, nullable: true }
                    address: { type: string, nullable: true }
      responses: { '201': { $ref: '#/components/responses/Created' } }
  /users/{user}:
    parameters: [{ name: user, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Users], summary: Show user, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: { tags: [Users], summary: Update user, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    patch: { tags: [Users], summary: Update user, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [Users], summary: Delete user, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /users/{user}/role:
    parameters: [{ name: user, in: path, required: true, schema: { type: integer } }]
    patch:
      tags: [Users]
      summary: Change user role
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role]
              properties: { role: { type: string, enum: [super-admin, admin, agent] } }
      responses: { '200': { $ref: '#/components/responses/Ok' } }
  /users/{user}/toggle-active:
    parameters: [{ name: user, in: path, required: true, schema: { type: integer } }]
    patch: { tags: [Users], summary: Toggle user active state, responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /users/{user}/reset-password:
    parameters: [{ name: user, in: path, required: true, schema: { type: integer } }]
    post:
      tags: [Users]
      summary: Reset user password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [password]
              properties: { password: { type: string, minLength: 8 } }
      responses: { '200': { $ref: '#/components/responses/Ok' } }
  /settings:
    get: { tags: [Settings], summary: List settings, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post:
      tags: [Settings]
      summary: Create setting
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [key, value]
              properties: { key: { type: string }, value: { type: string } }
      responses: { '201': { $ref: '#/components/responses/Created' } }
  /settings/{setting}:
    parameters: [{ name: setting, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Settings], summary: Show setting, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: &settingUpdate
      tags: [Settings]
      summary: Update setting value
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [value]
              properties: { value: { type: string } }
      responses: { '200': { $ref: '#/components/responses/Ok' } }
    patch: *settingUpdate
    delete: { tags: [Settings], summary: Delete setting, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /pre-order-cars:
    get:
      tags: [PreOrderCars]
      summary: List pre-order cars
      parameters:
        - { name: status, in: query, schema: { type: string, enum: [draft, pending, completed] } }
        - { name: supplier_id, in: query, schema: { type: integer } }
        - { $ref: '#/components/parameters/PerPage' }
      responses: { '200': { $ref: '#/components/responses/Ok' } }
    post: { tags: [PreOrderCars], summary: Create pre-order car draft, requestBody: { $ref: '#/components/requestBodies/JsonPreOrderCar' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /pre-order-cars/import:
    post: { tags: [PreOrderCars], summary: Import pre-order cars from xlsx/xls/csv, requestBody: { $ref: '#/components/requestBodies/PreOrderImport' }, responses: { '201': { $ref: '#/components/responses/Created' }, '422': { $ref: '#/components/responses/ValidationError' } } }
  /pre-order-cars/{preOrderCar}:
    parameters: [{ name: preOrderCar, in: path, required: true, schema: { type: integer } }]
    get: { tags: [PreOrderCars], summary: Show pre-order car, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: { tags: [PreOrderCars], summary: Update pre-order car, requestBody: { $ref: '#/components/requestBodies/JsonPreOrderCar' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [PreOrderCars], summary: Delete pre-order car, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /pre-order-cars/{preOrderCar}/publish:
    parameters: [{ name: preOrderCar, in: path, required: true, schema: { type: integer } }]
    post: { tags: [PreOrderCars], summary: Publish draft pre-order car, responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /pre-order-cars/{preOrderCar}/requests:
    parameters: [{ name: preOrderCar, in: path, required: true, schema: { type: integer } }]
    get: { tags: [PreOrderCars], summary: List customer requests for a pre-order car, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post:
      tags: [PreOrderCars]
      summary: Create customer request for pre-order car
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [customer_id]
              properties:
                customer_id: { type: integer }
                notes: { type: string, nullable: true }
      responses: { '201': { $ref: '#/components/responses/Created' } }
  /pre-order-cars/{preOrderCar}/requests/{preOrderCarRequest}:
    parameters: [{ name: preOrderCar, in: path, required: true, schema: { type: integer } }, { name: preOrderCarRequest, in: path, required: true, schema: { type: integer } }]
    delete: { tags: [PreOrderCars], summary: Cancel pending pre-order request, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
  /pre-order-cars/{preOrderCar}/requests/{preOrderCarRequest}/approve:
    parameters: [{ name: preOrderCar, in: path, required: true, schema: { type: integer } }, { name: preOrderCarRequest, in: path, required: true, schema: { type: integer } }]
    post: { tags: [PreOrderCars], summary: Approve pre-order request and create real order, responses: { '200': { $ref: '#/components/responses/Ok' } } }
  /pre-order-cars/{preOrderCar}/requests/{preOrderCarRequest}/reject:
    parameters: [{ name: preOrderCar, in: path, required: true, schema: { type: integer } }, { name: preOrderCarRequest, in: path, required: true, schema: { type: integer } }]
    post: { tags: [PreOrderCars], summary: Reject pre-order request (disabled — all requests are approved individually), responses: { '422': { $ref: '#/components/responses/ValidationError' } } }
  /contacts:
    get: { tags: [Contacts], summary: List contacts, parameters: [{ $ref: '#/components/parameters/Search' }, { $ref: '#/components/parameters/PerPage' }], responses: { '200': { $ref: '#/components/responses/Ok' } } }
    post: { tags: [Contacts], summary: Create contact, requestBody: { $ref: '#/components/requestBodies/JsonContact' }, responses: { '201': { $ref: '#/components/responses/Created' } } }
  /contacts/{contact}:
    parameters: [{ name: contact, in: path, required: true, schema: { type: integer } }]
    get: { tags: [Contacts], summary: Show contact, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    put: { tags: [Contacts], summary: Update contact, requestBody: { $ref: '#/components/requestBodies/JsonContact' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    patch: { tags: [Contacts], summary: Update contact, requestBody: { $ref: '#/components/requestBodies/JsonContact' }, responses: { '200': { $ref: '#/components/responses/Ok' } } }
    delete: { tags: [Contacts], summary: Delete contact, responses: { '200': { $ref: '#/components/responses/Deleted' } } }
